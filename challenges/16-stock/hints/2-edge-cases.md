## Hint 2: Handling Edge Cases

Remember to handle the case where the price hasn't changed (numericalChange === 0). In this case, colorClass should be undefined and arrow should be "▬".

## Hint 3: String Conversion

The challenge mentions converting numbers to strings. This happens naturally when you use `.toFixed(2)` or when rendering numbers in JSX.